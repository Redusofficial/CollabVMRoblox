-- TurnStatus.lua
-- Enumeration representing turn status states

local TurnStatus = {
    NONE = "none",
    REQUESTED = "requested",
    ASSIGNED = "assigned",
    ENDED = "ended",
}

return TurnStatus
